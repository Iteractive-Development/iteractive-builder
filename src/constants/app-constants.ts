// Application-related constants
export const RECENT_APPS_LIMIT = 10;

// Authentication-related constants
export const TOKEN_REFRESH_INTERVAL = 60 * 60 * 1000; // 1 hour (check less frequently since tokens last 24h)